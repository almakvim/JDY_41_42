{
  "name": "electron-tree-defaulte",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . terminal_tester --asar --platform=win32 --arch=x64 --prune=true --out=release-builds --icon=./img/terminal_m.ico",
    "package-overwrite": "electron-packager . terminal_tester --asar --platform=win32 --arch=x64 --prune=true --out=release-builds --icon=./img/terminal_m.ico --overwrite"
  },
  "keywords": [
    "electron"
  ],
  "author": "kvim",
  "license": "ISC",
  "devDependencies": {
    "electron": "^17.0.1",
    "electron-packager": "^15.5.1"
  },
  "dependencies": {
    "-": "^0.0.1",
    "anychart-nodejs": "^1.3.8",
    "axios": "^1.6.5",
    "electron-browser-storage": "^1.0.7",
    "electron-json-storage": "^4.5.0",
    "electron-store": "^8.0.2",
    "jquery": "^3.6.0",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.9",
    "save": "^2.4.0",
    "serialport": "^10.4.0"
  }
}
